# AGENTS.md

Before modifying any code, read `IMPLEMENTATION_RULES.md` in the repository root.

Follow `IMPLEMENTATION_RULES.md` as the project-level implementation contract.
Do not infer architecture that conflicts with it.
If the requested task conflicts with the rules, stop and report the conflict before editing files.